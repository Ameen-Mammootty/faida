# faida web

The review screen and dashboard. Arrives in **M3** (see `/plan.md`).
Next.js app, deployed on Vercel. Intentionally empty until then — no
endpoint gets built before the screen that consumes it, and vice versa.
