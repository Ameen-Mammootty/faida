import Incentive from "@/components/Incentive";

/**
 * M13.2 (issue #8): the incentive screen - the role shares block, the first
 * of the screen's blocks and the first door beside it.
 */
export const metadata = { title: "Faida - Incentive" };

export default function IncentivePage() {
  return <Incentive />;
}
